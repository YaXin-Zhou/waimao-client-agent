# WorkBuddy 本地 Skill

这是内部使用版本，不用于公开市场发布。

## 使用方式

1. 先启动本地 API：`python scripts/serve_api.py`。
2. 在 WorkBuddy 的技能入口选择“导入本地技能包”。
3. 选择本目录或将 `local-skill` 压缩为 zip 后导入。
4. 确认 WorkBuddy 允许访问本项目目录和本地 Python CLI。

公开 Buddy 应用、公开 Connector 审核和公网部署不属于当前内部使用范围。
