# 外贸客户智能工作流：当前进度与剩余阶段

更新时间：2026-09-09

## 一、结论

从“核心功能开发”角度，当前版本已经完成主要开发闭环，不需要再增加新的业务模块。

从“真实环境交付”角度，还剩 1 个条件性验证阶段：

1. 真实客户回信验证。

正式部署和客户正式验收按当前决定跳过，不计入本轮开发完成条件。

## 二、已完成的核心链路

```text
填写获客条件
    ↓
搜索真实公司与官网
    ↓
官网背调与证据保存
    ↓
数据清洗、去重、评分
    ↓
生成首次开发信草稿
    ↓
人工审核
    ↓
阿里邮箱 IMAP 只读同步
    ↓
识别系统通知、退信和客户来信
    ↓
客户回复分类与跟进建议
    ↓
生成客户跟进草稿
    ↓
人工审核
    ↓
发送前安全检查
    ↓
二次确认后发送
```

当前已实现的安全边界：

- 不把没有来源的行业、规模和采购意向写成事实。
- 系统通知不会进入客户回复分析。
- 退信不会生成跟进草稿。
- 首次开发信与客户跟进草稿明确区分。
- 草稿必须人工审核。
- 发送前必须重新执行安全检查。
- 只有“已批准 + 检查通过 + 二次确认”才显示发送动作。
- 真实 SMTP 发送必须通过人工批准、安全检查和二次确认。

## 三、已完成阶段

### 阶段 1：客户档案、评分和状态审计

状态：已完成。

客户、评分、背调证据、状态转换和审计时间线已经持久化，并经过真实官网客户档案验证。

### 阶段 2：官网背调任务编排

状态：本地核心功能已完成。

已支持持久化任务、异步队列、步骤状态、超时、重试、幂等键、失败记录、进程重启恢复和前端进度展示。生产级分布式队列不在当前本地交付范围内。

### 阶段 3：阿里邮箱 IMAP 只读接入

状态：本地真实接入已完成。

已完成真实阿里邮箱只读连接、收件箱同步、Message-ID 去重、线程关联、同步审计和系统通知识别。当前真实收件结果为 1 封阿里云系统欢迎邮件，未识别为客户回信。

### 阶段 4：客户回复分析和跟进草稿

状态：核心开发已完成，等待真实客户回信验证。

已支持中文、英文、德文、法文、西文、意大利文和葡萄牙文的基础业务分类；支持意向、询价、交期、投诉、退信、拒绝和其他；支持风险等级、人工复核、跟进待办和客户跟进草稿生成。

### 阶段 5：发送安全闸门

状态：核心开发已完成，已完成一次真实 SMTP 发送验证。

已支持草稿批准、收件人检查、黑名单、每日额度、最近联系时间、附件规则、发送审计、发送确认和失败状态处理。前端已经验证未批准草稿不会显示发送按钮。

### 阶段 6：多语言邮件生成

状态：核心功能已完成。

任务默认使用 `auto` 语言策略，也可以明确指定语言。系统按任务配置、客户来信、官网语言和目标市场配置选择开发信或跟进邮件语言；无法确认时使用英语并标记人工确认。草稿持久化语言、来源和确认标记，中文预览不改变原文。详见 `docs/architecture/EMAIL_LANGUAGE_POLICY.md`。

## 四、剩余阶段

### 剩余阶段 A：真实客户回信验证

触发条件：邮箱中收到真实客户回复。

验证内容：

```text
同步客户回信
    → 确认不是系统通知或退信
    → 回复分类
    → 风险等级和人工复核标记
    → 创建跟进待办
    → 生成客户跟进草稿
    → 前端显示客户跟进草稿
```

验收证据：真实邮件 Message-ID、分类结果、原始邮件关联、跟进待办和草稿类型均可追溯。

说明：没有真实客户回信时，不使用虚构结果代替验收；这不会阻塞继续做代码开发，但会保留为未完成的真实环境验收项。

### 已完成阶段 B：专用邮箱 SMTP 发送验证

触发条件：用户明确允许向专用测试收件人发送邮件。

验证内容：人工批准 → 安全检查 → 二次确认 → SMTP 发送 → 测试邮箱收信 → 退信/失败恢复 → 发送审计。

当前状态：已完成一次受控真实发送。发送审计为 `sent`，目标为真实公开地址
`kontakt@solar-generatoren.de`，主题为 `[SMTP TEST] Controlled mailbox connectivity check`。
随后通过真实 IMAP 执行同步，邮箱实际仍只有 1 封阿里云系统欢迎通知，没有客户回信或退信；因此“SMTP 发出”已验证，“客户回信/退信”仍不能凭空判定。

### 阶段 C：正式部署与客户验收

状态：按当前决定跳过。

不在本轮执行正式主机部署、生产监控、备份恢复演练和客户签字验收；相关文档保留，未来若恢复交付必须重新开启该阶段。

## 五、当前代码和验证基线

- 全量测试：184 passed。
- 前端生产构建：通过。
- 阿里邮箱状态：已配置、IMAP 只读；真实 SMTP 发送验证已完成，发送开关已恢复默认关闭。
- 当前真实收件：1 封系统欢迎邮件，已标记为系统通知。
- 真实 SMTP 审计：1 次，状态 `sent`；未记录发送错误。
- 当前真实客户档案：已保存官网背调和评分结果。
- 最近阶段提交：`0155e18`。
- 工作区：本次抓取质量改动验证完成后提交并保持干净。

## 七、最新数据质量收口（2026-09-09）

- 搜索结果只保存真实标题或页面摘要，不把查询词当作产品匹配证据。
- 官网公开页面正文会保存为受长度限制的来源片段，并参与匹配判断。
- 读取历史客户档案时会清理图片文件名、占位邮箱等明显无效来源。
- Google 静态请求和无界面 Chrome 均受异常流量限制时，接口明确返回失败，不生成或补齐虚假客户。
- 可选 Bing 来源默认关闭，待真实相关性抽检通过后再启用。
- 已使用真实官网 `https://elmag.eu/en/home/company` 和真实 DeepSeek 完成多次配置化背调 UAT。公司类型、成立年份、员工规模和目标业务匹配均保留来源；字段即使被模型标记为 `reported`，只要任务配置了人工复核，运行仍进入 `review_required`，不会直接视为最终事实。
- 背调阶段会在同域官网有限采集联系、公司、产品、解决方案等相关页面；不跟随无关外站，且有固定页数上限。
- 本地真实回归运行 `8b9b51ba-46ae-4e6a-bc12-27d0a5fa93a0` 已保存 5 条 ELMAG 官网来源，其中包含配置关键词对应的产品页；评分为 70 分，因字段人工复核配置而进入 `review_required`。
- 最新本地运行 `3ea3213a-ad89-4b3e-a327-1896f1ebabfc` 仍保存 5 条真实官网来源；当目标业务字段返回 `unknown` 时，系统将证据状态标记为 `insufficient`，运行进入 `review_required`，评分为 65 分。
- 自定义字段的 `checked_at` 由系统实际核验时刻生成，不采信模型返回的时间戳，避免把模型输出日期伪装成核验时间。
- 抓取预算有限时，联系页、Impressum 和法律信息页优先于普通关于页，同时保留产品相关页；真实回归覆盖 ELMAG 和 solar-generatoren.de。
- 客户池前端已区分搜索入口不可用、未形成候选记录和官网无公开邮箱三类结果；搜索失败不会显示为成功的空搜索。
- 官网导航不足时增加同域站点地图索引回退，最多展开 4 个站点地图入口，继续遵守页面预算和同域边界。
- 网页邮箱采集支持常见反混淆写法，规范化后仍必须通过格式和占位地址清洗，并保留原始上下文证据。
- 客户池漏斗新增独立官网来源数和多来源核验数，搜索结果页不计入真实性证据。
- 搜索候选按规范化官网主机名去重，同一公司的不同页面不会重复占用候选名额。
- 客户详情新增基于来源 URL 的证据等级，区分无来源、仅搜索来源、单一官网来源和多来源核验。
- 客户档案单独统计同域来源与外部关联来源，外部来源不被静默当作同域事实。
- 外部关联来源显示为“已关联、待人工确认”，不会仅凭官网外链判定主体一致。
- 外部页面进入证据前需通过官网身份词交叉匹配，匹配失败的无关外链会被丢弃。
- 客户详情对公司名、国家、邮箱和产品提供独立字段核验状态，未找到时不自动补齐。
- 邮箱域名不一致、国家来源冲突会单独显示为“来源冲突”。
- 客户列表和详情共用来源清洗路径，历史无效证据不会只在详情页重新出现。
- 对脚本渲染、首页缺少可解析导航的网站，在站点地图回退之后补探测有限的常见联系/公司路径；仍严格遵守页面预算，并保持产品页优先。
- 真实官网回归再次覆盖 ELMAG 与 solar-generatoren.de：两站均在 5 页预算内完成采集；ELMAG 发现真实公开邮箱 `office@elmag.at`，solar-generatoren.de 未发现公开邮箱且不会补造邮箱。
- 同一家公司存在多个邮箱时优先选择官网域名邮箱作为主联系人；关联域名邮箱仍保留，但档案质量标记为需复核，避免把跨域地址直接当作已确认事实。
- 产品证据匹配改为英文/数字词边界匹配，并兼容常见单复数形式；不会因为关键词只是无关英文单词的一部分而误判产品匹配。
- 官网来源未出现搜索结果中的公司主体名称时，档案增加 `company_identity_unconfirmed` 标记并进入需复核状态；前端显示可读原因，不将其静默视为完整客户。
- Google 搜索结果的来源 URL 现在记录为真实搜索结果页；搜索标题、摘要和搜索页邮箱不能作为官网产品或官网公开邮箱证据。
- 开启“需要公开邮箱”时，没有同域官网来源的邮箱记录只能保留在候选池，不能进入合格客户数；官网背调来源齐全后才可重新评估。
- 任务配置了目标产品时，合格客户必须同时具备官网产品证据；历史评分字段不能绕过这一门槛，缺少证据的记录只保留在候选池。

## 六、标准开发流程

后续任何修改都按以下顺序执行：

```text
需求卡片
  → 领域模型
  → 接口契约
  → 实现
  → 单元测试
  → 集成测试
  → 前端页面验证
  → 文档更新
  → Git 提交
  → 明确下一阶段
```

## 八、当前下一步

下一步是继续验证真实搜索入口在当前网络环境下的可用性，并完善“搜索失败、官网无邮箱、官网抓取成功但邮箱为空”三种状态的前端提示和数据统计；任何搜索入口未拿到真实结果时都不生成客户记录。
